# All optional. The app works with none of these set.

# Route YouTube traffic through a proxy (needed only on datacenter hosts like Vercel;
# must be a RESIDENTIAL proxy, datacenter proxies are blocked the same way).
# PROXY_URL=http://user:pass@host:port
# PROXY_TIMEOUT_MS=8000
